node_modules/
**/runs/
my-estate.json
*.log
.DS_Store
.env

# python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
*.egg-info/
build/
dist/

# scratch / tooling (subagent-driven-development ledger, etc.)
.superpowers/

# Databricks App runtime config — managed only in the workspace (has env-var wiring
# that the workspace UI edits directly; committing it here caused sync conflicts).
fabric-audit-agent-app/app.yaml
